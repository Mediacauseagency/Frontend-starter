// This file should always be imported first.
// It assigns the ENV object to window, so that properties
// can be accessed globally.
window.ENV = {
  chartColors: ['tomato', 'teal', 'goldenrod'],
  chartFontFamily: 'Consolas, monaco, monospace',
  chartFontColor: 'grey'
}
